# Secrets — NEVER commit
.env

# Python junk
__pycache__/
*.pyc
.venv/

# OS junk
Thumbs.db
Desktop.ini

# Submission packaging — build artifact, rename before upload, don't version control
*.zip

# Reference material (org-provided brief, design references) — not authored by us
reference_materials/